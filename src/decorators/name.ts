
export function Name(name: any) {
    // return function (target, propertyKey?){
    //     decoratorMapper({
    //         target,
    //         propertyKey: propertyKey?.toString(),
    //         parameters: name,
    //         fn: (name, schema, propertyKey?) => {
    //             if (!propertyKey) {
    //                 schema. = name;
    //             } else {
    //                 const schemaProperties = schema.properties[propertyKey];
    //                 if (typeof schemaProperties === 'boolean') return;
    //                 if (!schemaProperties) schemaProperties.items = {};
    //                 schemaProperties.name = name;
    //             }
    //             return schema;
    //         },
    //         schemaDecorator: SchemaDecorators.Name,
    //     })
    // }
}
